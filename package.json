{
  "name": "rask-downloader",
  "version": "1.1.0",
  "private": true,
  "description": "Chrome MV3 extension: downloads a Rask project as MP4 with video from Original and audio from Localized.",
  "license": "GPL-2.0-or-later",
  "homepage": "https://github.com/jimmorisedu-boop/rask_downloader",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "test": "node --test tests/*.test.mjs",
    "vendor:build": "node scripts/build-vendor.mjs",
    "vendor:verify": "node scripts/verify-vendor.mjs",
    "zip": "node scripts/build-zip.mjs",
    "check": "npm run lint && npm test && npm run vendor:verify"
  },
  "devDependencies": {
    "eslint": "^9.37.0"
  }
}
