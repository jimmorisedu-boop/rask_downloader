# npm — vendor/ уже содержит собранные файлы, сами зависимости в репозитории
# не нужны. package.json и package-lock.json, наоборот, коммитятся: без них
# нельзя воспроизвести vendor/ и проверить, из чего он собран.
node_modules/

# собранные архивы для передачи
*.zip

.DS_Store
Thumbs.db
